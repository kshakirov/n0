[package]
name = "n0"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
napi = "2.16.0"         # Основные типы Node-API
napi-derive = "2.16.0"  # Тот самый макрос #[napi]

[build-dependencies]
napi-build = "2.1.3"    # Сборщик для проброса C ABI